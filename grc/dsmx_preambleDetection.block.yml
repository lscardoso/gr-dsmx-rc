# auto-generated by grc.converter

id: dsmx_preambleDetection
label: preambleDetection
category: '[dsmx]'

parameters:
-   id: channel
    label: Channel
    dtype: int
    default: channel

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import gnuradio.dsmx as dsmx
    make: dsmx.preambleDetection(${channel})
    callbacks:
    - set_new_channel(${channel})

documentation: |-
    *************************************************
          TEST TEST
        *************************************************

file_format: 1
